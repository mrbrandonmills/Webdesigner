[phases.setup]
nixPkgs = ["nodejs"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmd = "echo 'Skipping Next.js build - automation only'"

[start]
cmd = "npx tsx scripts/automation/watchdog.ts"
